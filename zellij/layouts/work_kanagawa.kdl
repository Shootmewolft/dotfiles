layout {
    tab name="base" focus=true {
        pane
    }

    default_tab_template {
        pane size=1 borderless=true {
            plugin location="file:~/.config/zellij/plugins/zjstatus.wasm" {
              format_left   "{mode} #[fg=#76946A,bold]{tabs}"
              format_center "#[fg=#76946A,bold]{session}"
              format_right  "{command_git_branch} {datetime}"
              format_space  ""

              border_enabled  "false"
              border_char     "─"
              border_format   "#[fg=#E6C384]{char}"
              border_position "top"

              mode_normal  "#[bg=#DCD7BA] "
              mode_tmux    "#[bg=#E6C384] "

              tab_normal   "#[fg=#54546D]{index}:{name} "
              tab_active   "#[fg=#D27E99,bold,italic]{index}:{name} "

              command_git_branch_command     "git rev-parse --abbrev-ref HEAD"
              command_git_branch_format      "#[fg=#76946A] {stdout} "
              command_git_branch_interval    "10"
              command_git_branch_rendermode  "static"

              datetime        "#[fg=#7FB4CA,bold] {format} "
              datetime_format "%A, %d %b %Y %H:%M"
              datetime_timezone "America/Bogota"
            }
        }
        children
        pane size=1 borderless=true {
            plugin location="zellij:status-bar"
        }
    }

}
