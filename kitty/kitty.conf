# Theming — same colors as Zed's "Kanagawa Wave Blur" theme (pulled straight from
# the theme's own JSON), so the terminal matches the editor exactly.
background            #101018
foreground            #DCD7BA
cursor                #DCD7BA
selection_background  #223249
selection_foreground  #DCD7BA

color0                #16161D
color1                #C34043
color2                #76946A
color3                #FF9E3B
color4                #658594
color5                #957FB8
color6                #9CABCA
color7                #DCD7BA
color8                #54546D
color9                #FF5D62
color10               #98BB6C
color11               #E6C384
color12               #7FB4CA
color13               #D27E99
color14               #A3D4D5
color15               #DCD7BA

# Transparency (blur comes from Hyprland's compositor-level blur, already enabled globally)
# Opacity matches Zed's Kanagawa Wave Blur background alpha (#101018c0 -> 0xc0/255)
background_opacity 0.75
dynamic_background_opacity yes

# Font
font_family      IosevkaTerm Nerd Font
font_size 13.0

# Cursor
cursor_shape beam
cursor_trail 1

# Padding
window_margin_width 8

# No stupid close confirmation
confirm_os_window_close 0

# Use the user's login shell (zsh)
shell .

# Copy
map ctrl+c    copy_or_interrupt

# Search
map ctrl+f   launch --location=hsplit --allow-remote-control kitty +kitten search.py @active-kitty-window-id
map kitty_mod+f   launch --location=hsplit --allow-remote-control kitty +kitten search.py @active-kitty-window-id

# Scroll & Zoom
map page_up    scroll_page_up
map page_down    scroll_page_down

map ctrl+plus  change_font_size all +1
map ctrl+equal  change_font_size all +1
map ctrl+kp_add  change_font_size all +1
map ctrl+minus       change_font_size all -1
map ctrl+underscore       change_font_size all -1
map ctrl+kp_subtract       change_font_size all -1
map ctrl+0 change_font_size all 0
map ctrl+kp_0 change_font_size all 0
